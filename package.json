{
  "name": "health-gateway",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "echo 'Build skip'"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}
